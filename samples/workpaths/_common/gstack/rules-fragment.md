### gstack Limits

- gstack works through supported agent-host skill systems. It is not a primary Clade agent.
- Do not claim gstack support for DeepSeek-TUI unless the user supplies evidence of a compatible host setup.
- Use gstack only when the task benefits from its installed workflow commands; otherwise continue with the agent's normal tools.
