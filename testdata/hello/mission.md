Say hello in a way that proves the agent is alive.

This is the minimal possible workpath: only a mission, no manifest, no
tools, no playbook, no rules. Used to test that the loader's defaults are
sane.
